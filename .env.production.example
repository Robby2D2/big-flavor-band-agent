# Production Environment Variables for BigFlavor Band Agent
# Copy this file to .env.production and fill in your actual values

# ============================================================================
# Database Configuration
# ============================================================================
POSTGRES_PASSWORD=your_secure_database_password_here

# ============================================================================
# LLM Configuration
# ============================================================================
# LLM Provider: 'anthropic' or 'ollama'
# - anthropic: Uses Claude API (costs money, best quality)
# - ollama: Uses local LLM (free, lower quality, requires more resources)
LLM_PROVIDER=ollama

# Anthropic API (only required if LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Ollama Configuration (only used if LLM_PROVIDER=ollama)
# Base URL for Ollama service (use http://ollama:11434 in Docker)
OLLAMA_BASE_URL=http://ollama:11434
# Model to use (examples: llama3.1:8b, mistral:7b, llama3.2:3b, codellama:13b)
# See https://ollama.com/library for all available models
OLLAMA_MODEL=llama3.1:8b

# ============================================================================
# Auth0 Configuration
# ============================================================================
# Your Auth0 domain (e.g., your-tenant.us.auth0.com)
AUTH0_DOMAIN=your-domain.auth0.com

# Auth0 API Identifier for backend validation
AUTH0_API_IDENTIFIER=https://your-api-identifier

# Auth0 Client ID for frontend
AUTH0_CLIENT_ID=your_client_id_here

# Auth0 Client Secret for frontend
AUTH0_CLIENT_SECRET=your_client_secret_here

# Auth0 Issuer Base URL (usually https://your-domain.auth0.com)
AUTH0_ISSUER_BASE_URL=https://your-domain.auth0.com

# Auth0 Base URL - your application URL (e.g., https://yourdomain.com or http://your-server-ip)
AUTH0_BASE_URL=http://localhost

# Auth0 Secret - a long, random string used to encrypt the session cookie
# Generate with: openssl rand -hex 32
AUTH0_SECRET=your_long_random_secret_string_here_minimum_32_characters

# ============================================================================
# Application Configuration
# ============================================================================
NODE_ENV=production
